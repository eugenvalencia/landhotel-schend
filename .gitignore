# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Astro SSG-Schicht (paralleler Build neben Vite) — Output + Type-Cache
dist-astro
.astro

# Wrangler lokaler Emulator-State (pages dev)
.wrangler

# TypeScript Build-Cache (Artefakt, regeneriert beim Build)
*.tsbuildinfo

# Fremder Paketmanager — Repo nutzt npm (package-lock.json). pnpm-Artefakte
# ignorieren, damit nicht versehentlich ein zweites Lockfile committet wird.
pnpm-lock.yaml
pnpm-workspace.yaml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Brand pack build output (regenerated by scripts/build-brand-pack.mjs)
dist/brand-pack/

# Env (Supabase keys live in Vercel + local .env, not in repo)
.env
.env.local

# Supabase CLI workspace
supabase/.temp/

# Claude Code per-developer settings (ganzer Ordner — settings, agents, skills sind lokal)
.claude/

# Roh-Foto-Uploads (lokales Backup der Originale, nicht deployen/committen)
/_originale-upload-2026-06-01/
/public/fotos/karin 01.02.2026/

# Karin-Lieferung 10.06.2026: Halte-Ordner mit WhatsApp-Chat-Screenshots (PRIVAT!)
# + Wellness-/Sauna-Foto, das erst nach Karins Freigabe gezeigt wird.
/_zimmer-fotos-2026-06-10-source/

# Hero-Video Roh-Quellen (Runway-Downloads, noch mit Wasserzeichen) — nicht deployen/committen.
# Das ausgelieferte Hero-Video liegt sauber unter /public/hero/.
/_hero-video-source/
/public/fotos/schend foto verbessert.png
